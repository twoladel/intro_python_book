def multiply(left, right):
    return left * right

def get_num(prompt):
    return float(input(prompt))

first_number = get_num('Enter the first number: ')
second_number = get_num('Enter the second number: ')
product = multiply(first_number, second_number)
print(f'{first_number} * {second_number} = {product}')

# Identify all of the function names and parameters, include line numbers:

function names:
    multiply - line 1, 9
    get_num - line 4, 7, 8
    float - line 5
    input - line 5
    print - line 10

parameters:
    left - line 1
    right - line 1
    prompt - line 4
    